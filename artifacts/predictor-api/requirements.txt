fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
bcrypt==4.2.1
PyJWT==2.9.0
httpx==0.27.2
python-multipart==0.0.9
pydantic>=2.10.3
pydantic-settings==2.7.0

# Training pipeline. These are heavy; the API serves predictions from the
# saved joblib artifacts and does not need to train at request time.
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
joblib>=1.4.0

# Postgres driver for hosted deployments; SQLite is used locally.
psycopg[binary]>=3.2.0
# Optional booster. Needs an OpenMP runtime (libgomp), which the Dockerfile
# installs; training degrades to the sklearn candidates without it.
xgboost>=2.1.0
